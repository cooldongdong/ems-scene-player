# 消防員場「事件腳本產生器」開發規格

## 0. 交付前提

- `dev.md`：本檔，定義產品行為、資料轉換規則與驗收條件。
- `scenario-data.md`：情境來源資料，描述各情境的環境、病患與突發狀況素材需求。

目標是互動 demo：使用者選擇環境、急救事件與突發狀況後，系統從素材庫組出一張可播放的情境卡。這是可運作的 prototype，不需要非技術人員上傳素材、帳號、資料庫或後端管理介面。

## 1. 資料來源與正規化

`scenario-data.md` 的每個二級情境標題（例如「住宅 OHCA」）與其表格，是唯一的內容來源。每張表固定含六類資訊：

| 來源欄位 | 產出 role | 預期媒體 |
| --- | --- | --- |
| 環境影片 | `environment_video` | video；可在影片失敗時 fallback 為 image |
| 環境聲音 | `ambient_sound` | audio 或刻意無素材 |
| 病患樣貌照片 | `patient_photo` | image |
| 病患聲音 | `patient_voice` | audio 或刻意無素材 |
| 突發狀況綠幕影片（約 10 秒） | `complication_video` | video |
| 突發狀況聲音（1–2 句話） | `complication_voice` | audio，並顯示台詞；綠幕是影片時只顯示台詞、不播音檔（見 §3） |

建置時要從情境名稱與內容，萃取並統一以下三種標籤維度：

- `environment`：實際場域，例如 `住宅客廳`、`出租套房`、`KTV 包廂`、`市區街道`、`工地`。
- `medicalEvent`：急救事件，例如 `心跳停止（OHCA）`、`外傷出血`、`骨折（墜落）`、`車禍多重傷`、`酒醉意識不清`、`自傷（割腕）`。
- `complication`：突發狀況，例如 `家屬崩潰哭喊`、`旁人嗆聲干擾`、`同事欲搬動傷患`、`肇事雙方爭吵`、`同伴咆哮威脅`、`情緒失控自傷`。

標籤名稱可為了選單一致性做上述正規化，但不能改變 `scenario-data.md` 的情境語意。每個原始情境都必須能由對應標籤組合重現：

| 原始情境 | 環境 | 急救事件 | 突發狀況 |
| --- | --- | --- | --- |
| 住宅 OHCA | 住宅客廳 | 心跳停止（OHCA） | 家屬崩潰哭喊 |
| 街道打架止血 | 市區街道 | 外傷出血 | 旁人嗆聲干擾 |
| 工地墜落骨折 | 工地 | 骨折（墜落） | 同事欲搬動傷患 |
| 街道車禍情境 | 市區街道 | 車禍多重傷 | 肇事雙方爭吵 |
| KTV 情境 | KTV 包廂 | 酒醉意識不清 | 同伴咆哮威脅 |
| 情侶吵架後自殺情境 | 出租套房 | 自傷（割腕） | 情緒失控自傷 |

## 2. 產出的素材資料庫

請產出 `scenario-data.yaml`。YAML 是生成後供人維護的資料格式，不是需求輸入；它比 JSON 適合放巢狀標籤、註解及多行 TTS prompt。

最小結構如下：

```yaml
options:
  environment:
    室內: [住宅客廳, 出租套房, KTV 包廂]
    室外: [市區街道, 工地]
  medicalEvent: [心跳停止（OHCA）]
  complication: [家屬崩潰哭喊]

assets:
  - id: ev001
    type: video              # video | audio | image | none
    role: environment_video
    file: home_livingroom.mp4  # 前端會依 role 補為 assets/environment_video/home_livingroom.mp4
    fallback: home_livingroom.jpg  # 可選；jpg/jpeg/png，亦可填檔名陣列依序嘗試
    tags:
      environment: [住宅客廳]
      medicalEvent: []
      complication: []
    description: 家中客廳
```

資料規則：

- `options` 決定選單顯示項目與順序；環境以室內／室外分組，其他維度為平鋪陣列。
- 每一筆素材都要有唯一且穩定的 `id`、`type`、`role`、`tags`、`description`；有檔案時填 `file`。
- `tags` 任一維度為空陣列 `[]`，表示該維度不限，可與任意選擇搭配。
- 判斷素材是影片／圖片／聲音時，**副檔名優先於 `type`**，`type` 只在副檔名認不出來時才用。
  真實素材常常換格式（例如綠幕從影片換成一張劇照），這樣只要換檔案就能用，`type` 一時沒跟著改也不會壞掉；
  但 YAML 仍應寫成正確的 `type`。
- `type: none` 且不填 `file`，表示該條件下**刻意沒有**那類素材；這和「資料漏建、查無素材」不同。例如來源寫「無」的住宅／套房環境聲音，以及 OHCA／自傷病患聲音，應以此方式表達。
- 突發狀況的影片與聲音要各有同一個 `complication` 標籤，才能保證兩者語意一致。
- 現有來源只提供每個原始情境一組素材；可依合理共用關係增加多標籤（例如外傷照片可同時供街道外傷與工地骨折使用），但不得捏造來源未描述的人物或病況。

建議素材目錄固定為：

```text
assets/
  environment_video/
  ambient_sound/
  patient_photo/
  patient_voice/
  complication_video/
  complication_voice/
```

若尚無真實影音，請生成或放入清楚標示的佔位檔：環境影片約 6 秒、突發狀況綠幕影片約 10 秒、照片可用含描述的 SVG、聲音可用簡單音調／雜訊。佔位檔的目的只是在未取得授權素材前，讓載入、顯示、播放與停止等操作都能完整驗收；收到真實素材後，只需替換檔案與更新 YAML 路徑，不必修改程式。

## 3. 前端功能

實作為純靜態、單頁中文（繁體中文）網站。建議 `index.html` 使用原生 HTML/CSS/JavaScript，避免框架建置流程與伺服器依賴。

### 控制區

- 環境：必選下拉選單，依 `options.environment` 顯示室內／室外群組。
- 急救事件：必選下拉選單。
- 突發狀況：可複選的核取項目。
- 按鈕：`產生情境`（首次產生後改顯示 `重新產生`）、`播放場景`、`全部停止`。
- 產生情境後可開啟獨立投影視窗。視窗應盡量以螢幕可用範圍開啟，並提供全螢幕按鈕，
  讓使用者可移到投影螢幕後進入全螢幕。
- 「疊加到投影視窗」開關放在病患樣貌照片卡內（照片下方），預設關閉；本次有病患照片時才會出現。
  同一區塊提供大小／水平／垂直三個滑桿與「重設版面」，調整時投影視窗要即時跟著變。
- 綠幕疊加控制放在各組的突發狀況綠幕影片卡內（影片下方），該筆素材是可播影片時才出現。
  提供大小／水平／垂直／去背四個滑桿、「預覽定位」與「重設版面」。綠幕沒有常駐開關，
  因為它是觸發式的：按該組的 `播放這組` 才疊上去。
- 初始時抓取並解析 `scenario-data.yaml`；資料尚未成功載入前，產生按鈕必須停用，並顯示載入或具體錯誤訊息。
  抓取要帶 `cache: 'no-cache'`，否則改完 YAML 重新整理會讀到瀏覽器快取的舊資料。

### 選取規則

對六個角色分別選一筆素材，套用下列規則：

1. 必要條件：`environment` 與 `medicalEvent` 必須命中使用者選擇，或其標籤為 `[]`。
2. 突發狀況為優先條件：已勾選的突發狀況，優先選 `complication` 有命中的素材；該欄為 `[]` 的通用素材只在沒有命中素材時才可選。反之，標有特定突發狀況的素材不得在未勾選該狀況時被選到。
3. `complication_video` 與 `complication_voice` 只有使用者至少勾選一個突發狀況時才顯示。單一次急救訓練可能要演複數個突發狀況，所以**勾選幾項就產生幾組**，不做隨機取一；每一組只能使用該項的素材，不能混配成不同事件。
4. 同一個突發狀況可再拆成好幾個**片段**，由素材的 `segment` 數字決定（未填＝第 1 段）。一組要列出該狀況目前有的所有片段，依 segment 由小到大排序；每段的綠幕素材與台詞必須來自同一個 segment。同一個 segment 底下有多筆素材時，仍照一般規則隨機挑一筆。
5. 同一角色的候選素材隨機挑選。重新產生時，若還有其他候選，應避開該角色前一次的 `id`；拆成多段時，各段分開記各自上一次的 `id`。
6. 沒有候選素材時，顯示可理解的「此條件下查無符合的素材」提示；不要讓程式拋錯或空白。

### 情境卡與播放

- 版面分兩區。上方是場景底層的四張卡，依固定順序顯示：環境影片、環境聲音、病患樣貌照片、病患聲音。
- 下方是突發狀況區：勾選的每一項各成一個群組，群組內含該項的綠幕素材與聲音，全部並列在控制台頁面上，
  讓教官在一次訓練中依現場節奏逐一觸發。群組標題顯示序號與突發狀況名稱。
  未勾選任何突發狀況時，該區顯示說明文字而非空白。
- 一組只有一個片段時，維持單純的「一組兩張卡」版面，群組標題附 `播放這組` 與 `停止`。
  拆成多段時，群組標題改為顯示片段數與整組的 `停止`，組內每段各自一個區塊，附 `片段 N` 標題與該段的
  `播放這段` 與 `停止`。
- 同一時間只能演一段：按任何一段的播放，要先停掉突發狀況區其他正在播的媒體（含其他組），
  但不能動到場景底層的環境／病患媒體。
- 版面分兩層：**同一個事件內的影片／照片／配音卡片由上而下排**，維持閱讀順序；
  **突發狀況群組彼此則排成自適應網格**（`repeat(auto-fit, minmax(min(420px, 100%), 1fr))`），
  六個事件不會把整頁拉成一長條。
- 素材預覽一律限高（約 190px），因為綠幕與病患素材是 720×1280 的直式圖，不限高時一張卡就比一個螢幕還長。
  控制台只需要認出「這是哪一筆素材」，要看細節是在投影視窗。
- 窄視窗要能降到單欄，且任何寬度都不可出現橫向捲動：素材路徑這類長字串必須可斷行（`overflow-wrap: anywhere`），
  否則會把網格軌道撐開。
- 卡片要顯示角色名稱、素材描述、`id`／路徑與候選數量；若命中突發狀況則標示「命中突發狀況」。
- `type: none` 顯示「此情境無此素材」；無候選才顯示「查無素材」，兩者不可混淆。
- 依 `type` 用 `<video controls>`、`<audio controls>`、`<img>` 顯示。`environment_video` 若為 `type: video` 且有 `fallback`（jpg/jpeg/png 路徑或陣列），影片載入失敗時要依序改顯示圖片；沒有可用 fallback 時，在該卡顯示缺少的檔案路徑。
- 一段演出只能有一個聲源，由該段的綠幕素材是哪一種決定：
  - **綠幕是影片**：用影片自己的音軌。`complication_voice` 卡不放播放器，改顯示「這一段用綠幕影片的原聲」；
    `line` 與 `tts` 照常顯示。該段沒有配音素材時也不算資料缺口，不可顯示成「查無素材」。
  - **綠幕是靜態劇照**：影片沒得播，就播 `complication_voice` 的音檔（一般播放器，行為同其他角色）。
  投影視窗的影片一律靜音，否則同一支影片會在控制台與投影各響一次。
- 靜態劇照沒有可播媒體，但仍要能觸發投影疊加，所以該段的播放鈕不可因為「沒有可播媒體」而停用。
- `播放場景` 只從頭播放場景底層四張卡的音訊與影片並同步投影視窗，不會同時引爆所有突發狀況群組；
  各群組的 `播放這組` 只播該組的影片與聲音；`全部停止` 讓所有媒體與投影暫停並歸零。
  沒有對應可播放媒體時，該按鈕停用。
- 投影視窗只呈現本次選到的環境影像，不顯示情境卡與操作介面。影片須靜音、自動循環並以
  `object-fit: cover` 滿版播放；環境圖片與影片 fallback 也須以 cover 滿版顯示。重新產生及
  主畫面的播放／停止操作要同步至已開啟的投影視窗。
- 開啟「疊加到投影視窗」後，病患照片須完整保留比例（不裁切）疊在環境上；大小為「照片可佔畫面的比例」
  （滑桿值同時當成 `max-width: N vw` 與 `max-height: N vh`），水平／垂直為照片中心點在畫面的百分比位置。
  關閉時立即隱藏病患層；重新產生情境時要換成新照片並沿用目前的大小與位置設定。
- 綠幕素材可以是影片，也可以是一張綠幕劇照（`complication_video` 允許 image），兩者都要能疊到投影上。
- 綠幕素材也要能疊到投影上，作法與病患照片一致（保留比例、大小為可佔畫面比例、水平／垂直為中心點百分比），
  但綠色必須即時去掉，不能要求先把素材轉成帶 alpha 的檔案：投影視窗用 `<canvas>` 逐幀重畫，
  把接近綠幕色（`#00b140`）的像素轉透明。判斷用 YCbCr 的色度距離而非直接比 RGB，才耐得住打光不均；
  容差由該組滑桿控制，容差外再做一段半透明軟邊，並抑制人物邊緣殘留的綠光。
  逐像素運算量以 canvas 內部寬度上限（960px）控制，不隨素材解析度上升。
- 綠幕疊加的生命週期：**疊上去一律先停在第一格**，播放模式接著播一次並**停在最後一格**，
  兩種狀態都不會自己消失。只有按該段／該組的 `停止`、`全部停止` 或重新產生情境才會移除。
  同一時間只能有一段疊在投影上，改播另一段要接替前一段。
  靜態劇照沒有長度，本來就是停著；不論影片停在哪一格或靜態劇照，都不可讓逐幀迴圈空轉。
- 「預覽定位」把該段綠幕疊上去並停在第一格，方便先調好大小位置再開始訓練；再按一次結束預覽。
  暫停（含播完停在最後一格）時調整容差或版面滑桿，仍要重畫當下那一幀。
- 大小／位置／容差**以突發狀況為單位共用一份**，同一組的所有片段套用同樣的設定——同一個人站在同一個位置，
  不該每段重調一次。因此各片段的滑桿要連動：在任一段拖動，同組其他段的滑桿與投影畫面都要跟著更新。
  設定在重新產生情境後沿用，不因換素材而重置。
- 病患照片與綠幕的大小／位置／容差要**依當下的 `environment_video` 素材存進 `localStorage`**（key 用素材 `id`，
  不是環境名稱：同一個環境可能有好幾張背景，人物該站的位置本來就不同）。任何一次調整或「重設版面」都要立刻寫入；
  產生情境時依這次選到的環境素材載入，沒存過就回到預設值。
  存的內容只有版面數值，不含「疊加到投影視窗」開關——每次訓練都應該從沒有疊加的乾淨畫面開始。
  讀取時要能容忍內容毀損或被人手改壞：解析失敗當作沒存過，數值一律夾回滑桿範圍，
  寫入失敗（無痕模式、容量已滿）只是記不起來，不能影響當次操作。
- 影片在還沒播過之前，瀏覽器不一定會把第一格交給 canvas，所以預覽必須先播一下再倒回開頭停住，
  否則真實素材的預覽會是空白。
- 第二次以後重新產生時，素材有更換的卡片可用短暫醒目效果提示；候選僅一筆時，明確說明重新產生不會變，避免誤判為故障。

## 4. 突發狀況台詞與 TTS

該段綠幕是影片時，聲音以**影片自帶的音軌**為準，`complication_voice` 的音檔不參與播放（見 §3），
但台詞與配音 prompt 仍要保留在卡片上，供現場演員對詞與日後補錄使用；
綠幕只有靜態劇照時，這裡的配音就是該段唯一的聲音，仍要正常播放。

`complication_voice` 除音檔外，應保留來源台詞於 `line`，並在卡片中醒目顯示。可選擇加入 `tts` 區塊，讓日後產生正式配音：

```yaml
tts:
  model: Eleven v3
  voice: 20-30 歲台灣女性，聲線偏高、情緒幅度大
  settings: Stability = Creative，speed 1.0
  prompt: |
    [crying] 他到底怎麼了！你要救他啊！
```

- prompt 以台灣口吻寫成約 4–5 句，長度可配合約 10 秒綠幕影片；使用者可在卡片展開並一鍵複製。
- 車禍的緊急煞車聲是音效，應另外疊加，不要以 TTS 假裝生成。
- 涉及較重髒話時，先保留為可接受的較輕用語或註解，避免 TTS 平台過濾造成 demo 失敗。
- TTS 功能是輔助素材製作，不得阻擋核心情境產生與播放。

## 5. 本機執行與部署

因為瀏覽器直接以 `file://` 開啟時，常會封鎖 `fetch()` 讀取 YAML，請用任一靜態伺服器，例如：

```bash
python3 -m http.server 8000
```

再開啟 `http://localhost:8000`。成品沒有後端，可部署到 Cloudflare Pages、Netlify 或 GitHub Pages。媒體數量與解析度增加後，再評估靜態託管的頻寬與儲存配額。

## 6. 驗收清單

- 只用本檔與 `scenario-data.md` 能生成上述所有專案檔案，且不依賴 Google 服務。
- 本地 HTTP 伺服器下可成功讀取 YAML，沒有網路時仍可使用本地 YAML parser。
- 六個原始情境可用「環境＋急救事件＋對應突發狀況」重現，且六張卡的描述符合來源表格。
- 不勾突發狀況時，突發狀況區顯示「本次沒有突發狀況群組」；勾選 N 項就出現 N 組，每組的影片和聲音必須屬於同一項。
- 有 `segment` 的突發狀況會依序列出所有片段，每段的綠幕素材與台詞屬於同一段；沒有 `segment` 的資料維持一組兩張卡的舊版面。
- 演到一半按另一段（或另一組）的播放，前一段的聲音與影片要立刻停掉，不會兩段疊著響。
- 任何一段播放時，整個畫面只出一個聲音：綠幕是影片就是控制台的影片卡，綠幕是劇照就是突發狀況聲音卡。投影視窗永遠不出聲。
- 綠幕素材是靜態劇照時，該段沒有可播媒體，但播放鈕仍可按且會把人物疊上投影。
- 所有卡片的佔位或真實媒體可顯示／播放；媒體不存在時顯示明確錯誤，不影響其餘卡片。
- 六個突發狀況全勾時，整頁不應長到需要捲十幾個螢幕；事件在寬視窗並排成網格，縮到手機寬度時降為單欄且沒有橫向捲動。
- 改過 `scenario-data.yaml` 後只要重新整理就會讀到新資料，不需要清快取或強制重載。
- `播放場景`、各組的 `播放這組`、`全部停止`、重新產生與候選不足的提示皆可正常運作，且播放某一組不會連帶播到其他組。
- 投影視窗可開啟並盡量放大，環境影像滿版 cover；影片會靜音循環，且重新產生、播放與停止可同步。
- 病患照片疊加開關可即時顯示／隱藏病患層，且不會裁切透明人物圖片；拖動大小／位置滑桿時投影畫面即時更新，
  「重設版面」可回到預設值，重新產生情境後設定不會被重置。
- 綠幕素材疊上投影後停在第一格，按播放演完會停在最後一格且不會消失；人物去背疊在環境（與病患）之上，
  綠色不可殘留成方框。按停止、全部停止或重新產生才會移除，且同時最多只有一段在投影上。
- 綠幕的大小／位置滑桿與病患照片行為一致；去背滑桿調得太低會留下綠底、調到上限仍不該吃掉白色與深色制服。
- 「預覽定位」可停在第一格調版面（真實素材也要畫得出來，不能是空白），再按一次結束。
- 調好的版面關掉瀏覽器再開仍在；換到別的環境素材會變成該環境自己的一份，換回來又回到原本調好的位置。
  清掉或改壞 `localStorage` 只會回到預設值，不會讓頁面壞掉。
- 同一組的片段共用一份版面設定：在任一段拖滑桿，同組其他段的滑桿會跟著移動，投影也即時更新；不同組互不影響。
- 更換成真實素材時，只需要替換 `assets/` 檔案與調整 `scenario-data.yaml`，不需變更前端邏輯。
